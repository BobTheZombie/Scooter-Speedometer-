plugins { id 'com.android.application' }

android {
    namespace 'com.stankhouse.scooterspeedometer'
    compileSdk 35

    defaultConfig {
        applicationId 'com.stankhouse.scooterspeedometer'
        minSdk 23
        targetSdk 35
        versionCode 80
        versionName '8.0.0'
    }

    buildTypes {
        debug {
            applicationIdSuffix '.v8test'
            versionNameSuffix '-test'
        }
        release {
            minifyEnabled false
        }
    }
}
